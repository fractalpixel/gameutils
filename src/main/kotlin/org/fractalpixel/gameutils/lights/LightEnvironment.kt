package org.fractalpixel.gameutils.lights

/**
 * Provides functions for sending rays between two points and returning whether they are blocked, as well as
 * sending a ray in a direction and returning the light from there.
 */
interface LightEnvironment {


}